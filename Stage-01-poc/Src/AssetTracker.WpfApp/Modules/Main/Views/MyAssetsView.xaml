<UserControl x:Class="AssetTracker.WpfApp.Modules.Main.Views.MyAssetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetTracker.WpfApp.Modules.Main.Views"
             xmlns:commonv="clr-namespace:AssetTracker.WpfApp.Common.Views"
             xmlns:converters="clr-namespace:AssetTracker.WpfApp.Common.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Save Changes" Command="{Binding SaveCommand}" Margin="10" Padding="10,5"
                    IsEnabled="{Binding IsDirty, Mode=OneWay}"/>
        </StackPanel>
        
        
        <commonv:AssetsDataView Grid.Row="1" />
    </Grid>
</UserControl>
