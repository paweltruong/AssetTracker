<UserControl x:Class="AssetTracker.WpfApp.Common.Views.ScraperServiceListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetTracker.WpfApp.Common.Views"
             xmlns:vm="clr-namespace:AssetTracker.WpfApp.Common.ViewModels"
             xmlns:converters="clr-namespace:AssetTracker.WpfApp.Common.Converters"   
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:DesignTimeScraperServiceViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:ScraperServiceStatusToBrushConverter x:Key="ScraperServiceStatusToBrushConverter"/>
        <converters:ScraperServiceStatusToTextConverter x:Key="ScraperServiceStatusToTextConverter"/>

        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5 ,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
           CornerRadius="6" 
           Background="{TemplateBinding Background}"
           BorderBrush="{TemplateBinding BorderBrush}"
           BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Rounded Border Header with Information -->
        <Border Grid.Row="0" 
                CornerRadius="10,10,10,10" 
                Background="White" 
                BorderBrush="#DDDDDD" 
                BorderThickness="1,1,1,0"
                Margin="10,10,10,0"
                Padding="15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon Section -->
                <Border Grid.Column="0" 
                        Width="40" 
                        Height="40" 
                        Background="Transparent" 
                        CornerRadius="8"
                        Margin="0,0,15,0">
                    <Image Source="{Binding Model.IconUrl}"
                           Width="40" 
                           Height="40"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>

                <!-- Information Section -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <TextBlock Grid.Row="0" 
                               Text="{Binding Model.Title}" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="#333333"
                               Margin="0,0,0,2"/>

                    <!-- Description -->
                    <TextBlock Grid.Row="1" 
                               Text="{Binding Model.Description}"
                               FontSize="12" 
                               Foreground="#666666"
                               TextWrapping="Wrap"
                               Margin="0,2,0,2"/>

                    <!-- Status -->
                    <Border Grid.Row="2" 
                            Background="{Binding Model.Status, Converter={StaticResource ScraperServiceStatusToBrushConverter}}" 
                            CornerRadius="4" 
                            Padding="8,2" 
                            HorizontalAlignment="Left"
                            Margin="0,2,0,0">
                        <TextBlock Text="{Binding Model.Status, Converter={StaticResource ScraperServiceStatusToTextConverter}}" 
                                   FontSize="11" 
                                   Foreground="White" 
                                   FontWeight="SemiBold"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Button Panel -->
        <Border Grid.Row="1" 
                Background="Transparent" 
                Margin="10,5,10,10"
                Padding="10,0">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                <Button Content="{Binding Model.ScrapeDataButtonText}" 
                        Style="{StaticResource RoundedButtonStyle}"
                        Command="{Binding ConfigureCommand}"/>
                <Button Content="Open File" 
                        IsEnabled="False"
                        Style="{StaticResource RoundedButtonStyle}"
                        Command="{Binding LoadFileCommand}"/>
                <Button Content="Save To File" 
                        IsEnabled="False"
                        Style="{StaticResource RoundedButtonStyle}"
                        Command="{Binding SaveFileCommand}"/>
                <Button Content="{Binding Model.ViewDataButtonText}" 
                        Style="{StaticResource RoundedButtonStyle}"
                        Command="{Binding ViewDataCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>