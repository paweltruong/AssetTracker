<UserControl x:Class="AssetTracker.WpfApp.Common.Views.DefaultBrowserAssetsImporterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetTracker.WpfApp.Common.Views"       
             xmlns:converters="clr-namespace:AssetTracker.WpfApp.Common.Converters"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanInverterConverter x:Key="BooleanInverterConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5 ,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
           CornerRadius="6" 
           Background="{TemplateBinding Background}"
           BorderBrush="{TemplateBinding BorderBrush}"
           BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Margin="10">
            <Run Text="{Binding ImportDescription,Mode=OneWay}" FontWeight="Bold"/>
            <LineBreak/>           
            <Hyperlink NavigateUri="{Binding ImportSourceUrl,Mode=OneWay}" 
                       Command="{Binding OpenLinkCommand}" 
                       CommandParameter="{Binding ImportSourceUrl,Mode=OneWay}">
                GoTo Source Page
            </Hyperlink>
        </TextBlock>

        <wv2:WebView2 Grid.Row="1" Name="Browser" Source="https://syntystore.com/account/login"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                       Loaded="Browser_Loaded" />

        <StackPanel Grid.Row="2">
            <Button  Content="Scrape Data" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="10" Width="150" Click="ScrapeButton_Click"/>
            <Button  Content="Scrape Data2" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="10" Width="150" Click="ScrapeButton2_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="3" 
                   Text="{Binding StatusMessage}" 
                   FontSize="12" 
                   Foreground="#666666" 
                   Margin="10,0,10,10"
                   TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Grid.Row="4">
            <Button Content="Stop" 
                    Style="{StaticResource RoundedButtonStyle}"
                    Command="{Binding StopCommand}"/>
            <Button Content="Start" 
                    Style="{StaticResource RoundedButtonStyle}"
                    Command="{Binding StartCommand}"/>
        </StackPanel>

        <ProgressBar Grid.Row="5" 
                     Height="20"
                     IsIndeterminate="{Binding IsBusy}"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>
